import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
    flag : {
        height: '17px',
        border: '1px solid #94A3BB',
        borderRadius: '4px',
        padding: 0,
        margin: 0
    }
  });